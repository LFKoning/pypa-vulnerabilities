"""Module with setting for generating constraints."""

# Packages for which discrete version constraints must be created.
EXACT = [
    "numpy",
    "pandas",
    "scikit-learn",
]

# Packages for which no constraints should be created.
EXCLUDE = []

# Manual constraints to be included as-is.
MANUAL = []
